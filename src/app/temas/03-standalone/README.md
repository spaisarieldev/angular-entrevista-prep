# 03 — Standalone

Componentes/direcciones/pipes sin NgModule. Imports explícitos en el decorador.

## Ejemplo con Persona

Mostrar una persona usando `DatePipe` importado en el componente.

## Preguntas típicas

- Diferencia NgModule vs standalone
- Cómo se hace lazy load de un standalone component
- Qué va en `imports: []`
