# 08 — HttpClient + RxJS

Streams asíncronos para datos remotos.

## Ejemplo con Persona

Mock `PersonaApiMock` + `map` para filtrar activas + `async` pipe.

## Preguntas típicas

- `switchMap` vs `mergeMap` vs `concatMap` vs `exhaustMap`
- Por qué unsubscribe / `async` pipe / `takeUntilDestroyed`
- Interceptors
