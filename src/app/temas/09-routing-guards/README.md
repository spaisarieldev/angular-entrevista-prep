# 09 — Routing / guards

Navegación, lazy load y protección de rutas.

## Ejemplo con Persona

Guard funcional que mira el `rol` de una persona demo.

## Preguntas típicas

- `CanActivate` / `CanMatch` / `Resolve`
- Lazy loading con `loadComponent` / `loadChildren`
- `RouterLink` vs `navigateByUrl`
