# 01 — Control flow

Nuevo control flow de templates (`@if`, `@for`, `@switch`, `@empty`).

## Ejemplo con Persona

Listar `PERSONAS_DEMO`, filtrar activas/inactivas y mostrar el rol con `@switch`.

## Preguntas típicas de entrevista

- Diferencia entre `*ngIf` / `*ngFor` y el control flow nuevo
- Para qué sirve `track` en `@for`
- Qué hace `@empty`
