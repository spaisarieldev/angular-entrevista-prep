# 05 — Signals

Estado reactivo fine-grained sin Zone.js obligatorio.

## Ejemplo con Persona

Lista en un `signal`, selección y toggle de `activa` con `update`.

## Preguntas típicas

- `signal` vs `BehaviorSubject`
- `set` vs `update`
- Por qué los signals mejoran change detection
