# 02 — @defer

Carga diferida de partes del template (mejora LCP / bundle inicial).

## Ejemplo con Persona

Mostrar el detalle de una persona recién al interactuar.

## Preguntas típicas

- Triggers: `idle`, `viewport`, `interaction`, `hover`, `timer`
- Diferencia entre `@placeholder`, `@loading` y `@error`
- Relación con lazy loading de rutas
