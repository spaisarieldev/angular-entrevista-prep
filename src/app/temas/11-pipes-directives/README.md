# 11 — Pipes & directives

Transformación de datos en template y comportamiento en el DOM.

## Ejemplo con Persona

- Pipe: `apellido, nombre`
- Directiva: resalta si `activa`

## Preguntas típicas

- Pure vs impure pipes
- Attribute vs structural directives
- `HostBinding` / `HostListener` vs `host: {}`
