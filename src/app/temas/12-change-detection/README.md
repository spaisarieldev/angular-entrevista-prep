# 12 — Change detection

Cómo Angular detecta y renderiza cambios (Default vs OnPush, signals, zoneless).

## Ejemplo con Persona

Lista en `signal` dentro de un componente `OnPush`.

## Preguntas típicas

- Default vs OnPush
- Inmutabilidad / nuevas referencias
- Zone.js vs zoneless + signals
