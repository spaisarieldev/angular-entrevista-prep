# 10 — Dependency Injection

Providers, jerarquía de inyectores y `inject()`.

## Ejemplo con Persona

`PersonaService` con `listar()` y `porId()`.

## Preguntas típicas

- `providedIn: 'root'` vs providers del componente
- `inject()` vs constructor injection
- Tokens (`InjectionToken`) y `useClass` / `useFactory`
