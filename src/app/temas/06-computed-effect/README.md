# 06 — computed / effect

Valores derivados y side-effects reactivos sobre signals.

## Ejemplo con Persona

Filtrar activas con `computed` y loguear cantidad con `effect`.

## Preguntas típicas

- Cuándo usar `computed` vs método del template
- `effect` no debe usarse para derivar estado (usar `computed`)
- `untracked` / `allowSignalWrites` (contexto avanzado)
